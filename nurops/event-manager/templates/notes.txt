Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }}
  $ helm get all {{ .Release.Name }}

The Event Manager service is now running and provides:

- HTTP endpoint for receiving events: http://{{ .Release.Name }}.{{ .Release.Namespace }}.svc.cluster.local:80/events
- WebSocket endpoint for real-time event streaming: ws://{{ .Release.Name }}.{{ .Release.Namespace }}.svc.cluster.local:80/ws
- Health check endpoint: http://{{ .Release.Name }}.{{ .Release.Namespace }}.svc.cluster.local:80/health
- Event history endpoint: http://{{ .Release.Name }}.{{ .Release.Namespace }}.svc.cluster.local:80/events

To configure Alertmanager to send alerts to this service, add this to your Alertmanager config:

receivers:
  - name: 'event-manager'
    webhook_configs:
      - url: 'http://{{ .Release.Name }}.{{ .Release.Namespace }}.svc.cluster.local:80/events'
        send_resolved: true 